#pragma once
#include <utility>

template <typename T>
std::pair<T, T> cartesian_to_polar(T dx, T dy)
{
    //Do conversion here.

    return std::pair<T, T>(0, 0); //Returns pair.first = theta, pair.second = magnitude.
}
